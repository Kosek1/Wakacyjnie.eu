{
  "name": "ogarnijwakacje-website",
  "private": true,
  "description": "Build skrypty dla ogarnijwakacje.pl — kompiluje Tailwind CSS (zamiast wolnego cdn.tailwindcss.com) i generuje data/articles.json + sitemap.xml. Uruchamiane automatycznie przez Netlify przy każdym deployu (patrz netlify.toml).",
  "scripts": {
    "build:css": "tailwindcss -i ./styles/tailwind.css -o ./assets/css/tailwind.css --minify",
    "build": "npm run build:css && node build.js"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
